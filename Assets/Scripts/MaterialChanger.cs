using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MaterialChanger : MonoBehaviour
{
    // Start is called before the first frame update
    /*
    void Start()
    {
        List<Texture2D> list = ImageList.images;

        if (list != null && list.Count > 0)
        {
            Debug.Log("The imageList was passed succesfully with " + list.Count + "Images.");

            Texture2D texture2D = ImageList.images[0];

            Material material = CreateMaterailFromTexture2D.create(texture2D);

            if (material != null)
            {
                Renderer renderer = GetComponent<Renderer>();
                if (renderer != null)
                {
                    renderer.material = material;
                }
                else
                {
                    Debug.LogError("GameObject hat no renderer component");
                }

            }
        }
    }
    */
}
